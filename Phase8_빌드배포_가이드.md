# 시간외근무관리 앱 - Phase 8: 빌드 및 배포 가이드

## 📋 사전 확인

현재 package.json 설정은 정상입니다. 아래 순서대로 진행하시면 됩니다.

---

## 📌 Step 1: 앱 아이콘 확인

빌드하려면 `assets/icon.ico` 파일이 반드시 필요합니다.

```powershell
# 아이콘 파일 존재 여부 확인
dir C:\overtime-system\assets\
```

✅ `icon.ico`가 있으면 → Step 2로 이동
❌ 없으면 → HR 앱의 아이콘을 임시로 복사하거나, 새로 제작

```powershell
# HR 앱 아이콘 임시 복사 (새 아이콘 제작 전까지)
Copy-Item "C:\hr-system\assets\icon.ico" "C:\overtime-system\assets\icon.ico" -Force
```

> 💡 별도 아이콘을 만들고 싶으면 나중에 교체 가능합니다.
> 권장 크기: 256x256 이상, ICO 형식

---

## 📌 Step 2: .gitignore 확인

```powershell
# .gitignore 파일 확인
type C:\overtime-system\.gitignore
```

아래 내용이 포함되어 있어야 합니다. 없으면 새로 만드세요:

```powershell
# .gitignore 파일 생성 (없는 경우에만)
@"
node_modules/
dist/
*.log
.DS_Store
Thumbs.db
"@ | Out-File -FilePath "C:\overtime-system\.gitignore" -Encoding utf8
```

---

## 📌 Step 3: GitHub 저장소 생성

### 3-1. GitHub에서 저장소 생성

1. 브라우저에서 https://github.com/new 접속
2. 아래 설정으로 생성:
   - **Repository name**: `overtime-system`
   - **Description**: `시간외근무관리 데스크톱 앱`
   - **Public** 선택 (자동 업데이트에 필요)
   - **Initialize with README**: ❌ 체크 해제 (비어있는 상태로 생성)
3. **Create repository** 클릭

### 3-2. 로컬 저장소 초기화 및 Push

```powershell
cd C:\overtime-system

# Git 초기화 (이미 되어있으면 건너뛰기)
git init

# 원격 저장소 연결
git remote add origin https://github.com/mangfeel/overtime-system.git

# 이미 연결되어 있다면 위 명령이 에러 → 무시해도 됩니다
# 확인: git remote -v

# 모든 파일 추가
git add .

# 첫 커밋
git commit -m "feat: 시간외근무관리 데스크톱 앱 v1.0.0 초기 릴리즈"

# main 브랜치로 Push
git branch -M main
git push -u origin main
```

> ⚠️ Push 시 GitHub 로그인 팝업이 뜰 수 있습니다. 로그인하세요.
> ⚠️ `node_modules/`와 `dist/`는 .gitignore에 의해 자동 제외됩니다.

### 3-3. Push 성공 확인

브라우저에서 https://github.com/mangfeel/overtime-system 접속하여 파일이 올라갔는지 확인합니다.

---

## 📌 Step 4: 빌드 (exe 생성)

```powershell
cd C:\overtime-system

# 의존성 설치 (이미 되어있더라도 한번 더 실행)
npm install

# exe 빌드
npm run build
```

> ⏱️ 빌드는 2~5분 정도 소요됩니다.
> 처음 빌드 시 Electron 바이너리를 다운로드하므로 인터넷 연결 필요합니다.

### 빌드 결과 확인

```powershell
dir C:\overtime-system\dist\
```

정상적이면 아래 파일들이 생성됩니다:

```
dist/
├── 시간외근무관리 Setup 1.0.0.exe         ← 설치 파일
├── 시간외근무관리 Setup 1.0.0.exe.blockmap ← 자동 업데이트용
├── latest.yml                              ← 자동 업데이트 정보
└── win-unpacked/                           ← 포터블 실행 폴더
```

> ❌ 빌드 에러 발생 시 → 에러 메시지를 알려주세요.

---

## 📌 Step 5: exe 설치 테스트

### 5-1. 설치

```
dist\시간외근무관리 Setup 1.0.0.exe
```

를 더블클릭하여 설치합니다.

### 5-2. 설치 후 확인 사항

| 확인 항목 | 예상 결과 |
|---|---|
| 바탕화면/시작메뉴에 "시간외근무관리" 바로가기 | ✅ 생성됨 |
| 앱 실행 | ✅ 정상 실행 |
| 직원 목록 표시 | ✅ HR 앱 데이터 연동 |
| 시간외근무 데이터 표시 | ✅ 기존 데이터 유지 |
| DevTools (F12) | ❌ 안 열려야 정상 (프로덕션) |

> ⚠️ 개발 모드에서 테스트할 때는 DevTools가 열렸지만,
> 빌드된 exe에서는 `isDev = false`이므로 자동으로 차단됩니다.

---

## 📌 Step 6: GitHub Release 발행

### 6-1. 파일명 변경 (영문)

GitHub Release에 한글 파일명은 문제를 일으킬 수 있으므로 영문으로 변경합니다.

```powershell
cd C:\overtime-system\dist

# 파일명 영문으로 변경
Rename-Item "시간외근무관리 Setup 1.0.0.exe" "OT-System-Setup-1.0.0.exe"
Rename-Item "시간외근무관리 Setup 1.0.0.exe.blockmap" "OT-System-Setup-1.0.0.exe.blockmap"
```

### 6-2. latest.yml 수정

자동 업데이트가 영문 파일명을 찾을 수 있도록 latest.yml을 수정해야 합니다.

```powershell
notepad C:\overtime-system\dist\latest.yml
```

`path:` 항목의 파일명을 영문으로 변경:

```yaml
# 변경 전
path: 시간외근무관리 Setup 1.0.0.exe

# 변경 후
path: OT-System-Setup-1.0.0.exe
```

저장 후 닫기.

### 6-3. Git Tag 생성

```powershell
cd C:\overtime-system

git tag v1.0.0
git push origin v1.0.0
```

### 6-4. GitHub Release 생성

1. 브라우저에서 https://github.com/mangfeel/overtime-system/releases/new 접속
2. 아래와 같이 설정:

| 항목 | 값 |
|---|---|
| **Tag** | `v1.0.0` (목록에서 선택) |
| **Release title** | `v1.0.0 - 시간외근무관리 초기 릴리즈` |
| **Description** | 아래 내용 복사 |

Release Description (복사용):

```markdown
## 시간외근무관리 데스크톱 앱 v1.0.0

### 주요 기능
- 시간외근무 등록 (일별/일괄 입력, 달력 뷰)
- 근태 등록 (붙여넣기 파싱, 달력 관리)
- 주간/월간 현황 (12시간 초과 감지)
- 월별 집계 (수당/대휴 자동 계산)
- 상세 보고서 / 대휴 보고서
- 엑셀 다운로드 / 인쇄
- .hrm 암호화 백업/복원
- 인사관리 앱 데이터 연동 (직원/급여)

### 설치 방법
1. `OT-System-Setup-1.0.0.exe` 다운로드
2. 실행하여 설치
3. ⚠️ 인사관리 앱이 먼저 설치되어 있어야 합니다

### 요구 사항
- Windows 10 이상
- 인사관리 앱 설치 필수
```

3. **파일 업로드** (드래그 앤 드롭):
   - `OT-System-Setup-1.0.0.exe`
   - `OT-System-Setup-1.0.0.exe.blockmap`
   - `latest.yml`

4. **Publish release** 클릭

---

## 📌 Step 7: 자동 업데이트 검증

이 단계는 **v1.0.1 이후 업데이트를 릴리즈할 때** 확인합니다.
v1.0.0은 첫 릴리즈이므로 지금은 건너뛰셔도 됩니다.

나중에 업데이트할 때의 절차:

```powershell
# 1. package.json 버전 올리기
# "version": "1.0.0" → "1.0.1"

# 2. 코드 수정 후 커밋
git add .
git commit -m "feat: v1.0.1 ○○ 기능 추가"
git push origin main

# 3. 빌드
npm run build

# 4. 파일명 변경
cd dist
Rename-Item "시간외근무관리 Setup 1.0.1.exe" "OT-System-Setup-1.0.1.exe"
Rename-Item "시간외근무관리 Setup 1.0.1.exe.blockmap" "OT-System-Setup-1.0.1.exe.blockmap"

# 5. latest.yml 파일명 수정 (path: OT-System-Setup-1.0.1.exe)

# 6. Tag & Release
git tag v1.0.1
git push origin v1.0.1
# GitHub Release 생성 + 3개 파일 업로드
```

기존 v1.0.0이 설치된 PC에서 앱을 실행하면 3초 후 자동으로 업데이트 알림이 뜹니다.

---

## 📌 완료 체크리스트

| # | 항목 | 확인 |
|---|---|---|
| 1 | assets/icon.ico 존재 | ⬜ |
| 2 | .gitignore 설정 | ⬜ |
| 3 | GitHub 저장소 생성 & Push | ⬜ |
| 4 | `npm run build` 성공 | ⬜ |
| 5 | exe 설치 테스트 통과 | ⬜ |
| 6 | GitHub Release 발행 (3개 파일) | ⬜ |
| 7 | 배포 URL 확인 | ⬜ |

모든 항목이 ✅이면 **Phase 8 완료**입니다! 🎉
