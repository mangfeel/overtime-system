# 시간외근무관리 데스크탑 앱 전환 로드맵

## 📋 문서 정보
- **작성일**: 2026년 2월 5일
- **버전**: v1.0
- **목표**: 개인용 시간외근무관리 웹앱 → Electron 데스크탑 앱 전환
- **예상 총 기간**: 15~20시간 (8단계)
- **현재 진행 상태**: 📋 계획 수립 완료

---

## 📖 배경 및 목적

### 1. 현재 상태 (개인용 웹앱)

```
[시간외근무관리.html] - 단일 HTML 파일 (11,710줄, 177개 함수)
    │
    ├── 데이터 저장 → 브라우저 localStorage
    ├── 직원 데이터 → 인사관리 웹앱의 localStorage에서 읽기
    ├── 코드 보호 → 없음 ⚠️
    └── 라이선스 → 없음 ⚠️
```

### 2. 목표 상태 (데스크탑 앱)

```
[시간외근무관리 Desktop App - Electron]
    │
    ├── 코드 보호 → Electron 패키징 (F12 비활성화) ✅
    ├── 라이선스 → 인사관리 앱 라이선스 검증 ✅
    ├── 직원 데이터 → 인사관리 앱 로컬 파일에서 읽기 ✅
    ├── 시간외/근태 데이터 → 자체 로컬 파일 저장 ✅
    └── 자동 업데이트 → GitHub Release 기반 ✅
```

### 3. 핵심 원칙

| 원칙 | 설명 |
|------|------|
| 독립 설치 | 인사관리 앱과 별도 설치 파일 (별도 GitHub 저장소) |
| 인사앱 의존 | 인사관리 앱 설치 + 유효 라이선스 필수 |
| 단방향 참조 | 인사앱 데이터 → 읽기 전용 (쓰기 불가) |
| 로컬 저장 | 시간외/근태 데이터는 자체 로컬 파일에만 저장 |
| 서버 미동기화 | 시간외 데이터는 서버에 올리지 않음 |

---

## 💻 폴더 구조

### 1. 로컬 PC 폴더 구조

#### C:\overtime-system (메인 작업 폴더)

```
C:\overtime-system\
│
├── package.json              # Electron 설정
├── main.js                   # Electron 메인 프로세스
├── preload.js                # 보안 브릿지 (IPC)
│
├── 메인_시간외.html            # 메인 애플리케이션
│
├── css/
│   └── 스타일_시간외.css       # 메인 스타일시트
│
├── js/
│   ├── 초기화_시간외.js         # 앱 초기화
│   │
│   ├── config/               # 설정/상수
│   │   └── 상수_시간외.js
│   │
│   ├── core/                 # 핵심 모듈
│   │   ├── 데이터베이스_시간외.js   # 로컬 파일 기반 데이터 관리
│   │   ├── 인사연동_시간외.js      # 인사관리 앱 데이터 읽기
│   │   └── 급여연동_시간외.js      # 급여표/직책수당 읽기
│   │
│   ├── overtime/             # 시간외근무 기능
│   │   ├── 연장근로등록_시간외.js   # 일별 연장근로 등록
│   │   ├── 주간현황_시간외.js      # 주간 현황 (12h 초과 감지)
│   │   ├── 월별집계_시간외.js      # 월별 집계
│   │   └── 보고서_시간외.js        # 상세 보고서 / 엑셀
│   │
│   ├── attendance/           # 근태 관리
│   │   ├── 근태등록_시간외.js      # 근태 데이터 등록
│   │   └── 근태달력_시간외.js      # 근태 달력 뷰
│   │
│   ├── settings/             # 설정
│   │   ├── 공휴일_시간외.js        # 공휴일 관리
│   │   ├── 유형설정_시간외.js      # 시간외 유형 설정
│   │   └── 제한설정_시간외.js      # 주/월 제한 설정
│   │
│   ├── system/               # 시스템 기능
│   │   ├── 백업_시간외.js
│   │   └── 복원_시간외.js
│   │
│   ├── license/              # 라이선스
│   │   └── license_시간외.js     # 인사앱 라이선스 검증
│   │
│   └── utils/                # 유틸리티
│       ├── 날짜유틸_시간외.js
│       ├── 포맷유틸_시간외.js
│       └── 인쇄유틸_시간외.js
│
├── assets/                   # 앱 리소스
│   ├── icon.ico
│   └── icon.png
│
└── dist/                     # 빌드 결과물
    ├── OT-System-Setup-x.x.x.exe
    └── latest.yml
```

### 2. 데이터 저장 경로

```
%APPDATA%/overtime-system/     ← Electron userData
├── data/
│   ├── overtime_daily.json      # 시간외근무 기록
│   ├── attendance_records.json  # 근태 기록
│   ├── holidays.json            # 공휴일 설정
│   ├── overtime_settings.json   # 시간외 유형 설정
│   └── limit_settings.json      # 제한 설정
│
├── backup/                      # 자동 백업 (최근 5개)
│   └── backup_20260205.json
│
└── license/
    └── license_cache.json       # 라이선스 캐시

%APPDATA%/hrm-system/           ← 인사관리 앱 userData (읽기 전용)
├── data/
│   ├── hr_system_v25_db.json    # 직원 데이터
│   ├── hr_salary_tables.json    # 급여표
│   ├── hr_salary_grades.json    # 급여등급
│   ├── hr_position_allowances.json  # 직책수당
│   ├── hr_salary_settings.json  # 급여 설정
│   ├── hr_ordinary_wage_settings.json  # 통상임금 설정
│   └── hr_concurrent_positions.json  # 겸직/직무대리
│
└── license/
    └── license_cache.json       # 라이선스 정보 (검증 참조)
```

---

## 🔗 데이터 연동 아키텍처

### 1. 인사관리 앱 → 시간외근무 앱 (읽기 전용)

```
┌─────────────────────────────────────────┐
│   인사관리 데스크탑 앱 (hrm-system)      │
│                                         │
│   %APPDATA%/hrm-system/data/            │
│   ├── hr_system_v25_db.json  ──────────────┐
│   ├── hr_salary_tables.json  ──────────────┤
│   ├── hr_position_allowances.json  ────────┤  읽기 전용
│   ├── hr_salary_settings.json  ────────────┤
│   └── hr_concurrent_positions.json  ───────┤
│                                         │  │
│   라이선스 정보 ─────────────────────────┤  │
└─────────────────────────────────────────┘  │
                                              │
┌─────────────────────────────────────────┐  │
│   시간외근무 데스크탑 앱 (overtime-system)│  │
│                                         │  │
│   ┌─ 인사연동_시간외.js ←───────────────────┘
│   │  (Electron IPC로 파일 읽기)
│   │
│   ├─ 자체 데이터 (읽기/쓰기)
│   │  %APPDATA%/overtime-system/data/
│   │  ├── overtime_daily.json
│   │  ├── attendance_records.json
│   │  └── ...
│   │
│   └─ 라이선스 검증 → Supabase
│      (인사앱과 동일 라이선스 키 사용)
└─────────────────────────────────────────┘
```

### 2. localStorage 키 → 로컬 파일 매핑

| 기존 localStorage 키 | 파일명 | 앱 | 접근 |
|---|---|---|---|
| `hr_system_v25_db` | hr_system_v25_db.json | 인사앱 | 읽기 전용 |
| `hr_salary_tables` | hr_salary_tables.json | 인사앱 | 읽기 전용 |
| `hr_position_allowances` | hr_position_allowances.json | 인사앱 | 읽기 전용 |
| `hr_salary_settings` | hr_salary_settings.json | 인사앱 | 읽기 전용 |
| `hr_ordinary_wage_settings` | hr_ordinary_wage_settings.json | 인사앱 | 읽기 전용 |
| `hr_concurrent_positions` | hr_concurrent_positions.json | 인사앱 | 읽기 전용 |
| `hr_overtime_daily` | overtime_daily.json | 시간외앱 | 읽기/쓰기 |
| `hr_attendance_records` | attendance_records.json | 시간외앱 | 읽기/쓰기 |
| `hr_overtime_holidays` | holidays.json | 시간외앱 | 읽기/쓰기 |
| `hr_overtime_settings` | overtime_settings.json | 시간외앱 | 읽기/쓰기 |
| `hr_overtime_limits` | limit_settings.json | 시간외앱 | 읽기/쓰기 |

### 3. 라이선스 검증 흐름

```
[시간외근무 앱 실행]
    │
    ▼
[인사관리 앱 설치 확인]
    │ → %APPDATA%/hrm-system/ 존재?
    │
    ├── 없음 → "인사관리 시스템이 설치되지 않았습니다" → 앱 종료
    │
    ▼ 있음
[인사관리 앱 라이선스 캐시 확인]
    │ → %APPDATA%/hrm-system/license/license_cache.json
    │
    ├── 없음/만료 → "인사관리 시스템에서 먼저 로그인해주세요" → 앱 종료
    │
    ▼ 유효
[Supabase 라이선스 온라인 검증] (인터넷 연결 시)
    │
    ├── 유효 → 앱 정상 실행 ✅
    ├── 만료 → "라이선스가 만료되었습니다" → 앱 종료
    └── 오프라인 → 캐시 만료일 기준으로 판단 (7일 유예)
```

---

## 📋 단계별 작업 계획

### Phase 1: 프로젝트 초기화 및 Electron 설정 (1~2시간)

**목표**: Electron 프로젝트 뼈대 구성

**작업 내용**:
1. `C:\overtime-system` 폴더 생성
2. `npm init -y` → `package.json` 설정
3. `npm install electron electron-builder --save-dev`
4. `main.js` 작성 (메인 프로세스)
5. `preload.js` 작성 (IPC 브릿지)
6. `메인_시간외.html` 기본 구조 작성 (빈 탭 네비게이션)
7. `npm start`로 빈 Electron 창 동작 확인

**핵심 파일**:
- `C:\overtime-system\main.js`
- `C:\overtime-system\preload.js`
- `C:\overtime-system\package.json`
- `C:\overtime-system\메인_시간외.html`

**main.js 주요 기능**:
```javascript
// IPC 핸들러 등록
ipcMain.handle('read-hr-data', (event, key) => { ... });    // 인사앱 데이터 읽기
ipcMain.handle('read-data', (event, key) => { ... });       // 자체 데이터 읽기
ipcMain.handle('write-data', (event, key, data) => { ... });// 자체 데이터 쓰기
ipcMain.handle('check-hr-app', () => { ... });              // 인사앱 설치 확인
ipcMain.handle('validate-license', () => { ... });          // 라이선스 검증
```

**preload.js 주요 API**:
```javascript
contextBridge.exposeInMainWorld('electronAPI', {
    readHRData: (key) => ipcRenderer.invoke('read-hr-data', key),
    readData: (key) => ipcRenderer.invoke('read-data', key),
    writeData: (key, data) => ipcRenderer.invoke('write-data', key, data),
    checkHRApp: () => ipcRenderer.invoke('check-hr-app'),
    validateLicense: () => ipcRenderer.invoke('validate-license'),
    exportFile: (data, filename) => ipcRenderer.invoke('export-file', data, filename),
    importFile: (filters) => ipcRenderer.invoke('import-file', filters),
    getAppVersion: () => ipcRenderer.invoke('get-app-version'),
    openInBrowser: (url) => ipcRenderer.invoke('open-in-browser', url)
});
```

**✅ 완료 기준**: `npm start`로 빈 Electron 창 실행 성공

---

### Phase 2: 라이선스 검증 시스템 (1~2시간)

**목표**: 인사관리 앱 라이선스를 통한 접근 제어

**작업 내용**:
1. `license_시간외.js` 작성
2. 인사관리 앱 설치 경로 감지
3. 라이선스 캐시 파일 읽기 및 검증
4. Supabase 온라인 검증 (기존 validate-license API 재사용)
5. 오프라인 모드 지원 (캐시 기반 7일 유예)
6. 라이선스 만료/미인증 시 차단 화면

**핵심 파일**:
- `C:\overtime-system\js\license\license_시간외.js`
- `C:\overtime-system\main.js` (라이선스 IPC 추가)

**검증 로직**:
```
1. %APPDATA%/hrm-system/ 디렉토리 존재 확인
2. license_cache.json 읽기
3. 만료일 확인 (캐시 기준)
4. 온라인 시 → Supabase validate-license API 호출
5. 결과에 따라 앱 실행 허용/차단
```

**✅ 완료 기준**: 인사앱 미설치 시 차단, 유효 라이선스로 앱 진입 성공

---

### Phase 3: 데이터 레이어 구축 (2~3시간)

**목표**: localStorage → 파일 기반 데이터 읽기/쓰기

**작업 내용**:
1. `데이터베이스_시간외.js` 작성 (자체 데이터 CRUD)
2. `인사연동_시간외.js` 작성 (인사앱 데이터 읽기 전용)
3. `급여연동_시간외.js` 작성 (급여표/직책수당 읽기)
4. `main.js`에 파일 I/O IPC 핸들러 구현
5. 데이터 자동 저장/로드 테스트

**핵심 파일**:
- `C:\overtime-system\js\core\데이터베이스_시간외.js`
- `C:\overtime-system\js\core\인사연동_시간외.js`
- `C:\overtime-system\js\core\급여연동_시간외.js`
- `C:\overtime-system\main.js` (IPC 핸들러)

**데이터베이스_시간외.js 핵심 API**:
```javascript
const OvertimeDB = {
    // 자체 데이터 (읽기/쓰기)
    async loadOvertimeRecords()     → overtime_daily.json 읽기
    async saveOvertimeRecords(data) → overtime_daily.json 쓰기
    async loadAttendance()          → attendance_records.json 읽기
    async saveAttendance(data)      → attendance_records.json 쓰기
    async loadHolidays()            → holidays.json 읽기
    async saveHolidays(data)        → holidays.json 쓰기
    async loadSettings()            → overtime_settings.json 읽기
    async saveSettings(data)        → overtime_settings.json 쓰기
    async loadLimits()              → limit_settings.json 읽기
    async saveLimits(data)          → limit_settings.json 쓰기
};

const HRDataReader = {
    // 인사앱 데이터 (읽기 전용)
    async getEmployees()            → hr_system_v25_db.json → employees[]
    async getActiveEmployees()      → 재직자만 필터
    async getSalaryTables()         → hr_salary_tables.json
    async getPositionAllowances()   → hr_position_allowances.json
    async getSalarySettings()       → hr_salary_settings.json
    async getConcurrentPositions()  → hr_concurrent_positions.json
    async isHRAppAvailable()        → 인사앱 데이터 접근 가능 여부
};
```

**✅ 완료 기준**: 인사앱 직원 데이터 읽기 + 자체 데이터 저장/로드 성공

---

### Phase 4: HTML 분리 및 메인 UI 구성 (2~3시간)

**목표**: 단일 HTML에서 CSS/HTML 분리 + 탭 네비게이션

**작업 내용**:
1. CSS를 `스타일_시간외.css`로 분리 (~600줄)
2. HTML 본문을 `메인_시간외.html`에 정리
3. 6개 탭 구조 유지:
   - 📈 주월간현황 (weekly)
   - 🕐 근태 등록 (attendance)
   - 📅 연장근로등록 (daily)
   - 📊 월별 집계 (monthly)
   - 📋 상세 보고서 (report)
   - ⚙️ 설정 (settings)
4. `초기화_시간외.js` 작성 (탭 전환, 앱 부팅 시퀀스)
5. `상수_시간외.js` 작성 (키보드 단축키, 상수 정의)

**핵심 파일**:
- `C:\overtime-system\css\스타일_시간외.css`
- `C:\overtime-system\메인_시간외.html`
- `C:\overtime-system\js\초기화_시간외.js`
- `C:\overtime-system\js\config\상수_시간외.js`

**메인_시간외.html 핵심 변경**:
- `<script src="https://cdn.jsdelivr.net/npm/exceljs">` → `npm install exceljs` (로컬)
- `localStorage.getItem()` → `await window.electronAPI.readData()` (비동기)
- `localStorage.setItem()` → `await window.electronAPI.writeData()` (비동기)
- `prompt()` 사용 없음 확인 (Electron 미지원)
- `alert()` → 커스텀 모달로 교체 고려

**✅ 완료 기준**: 빈 탭 UI가 Electron 창에서 정상 표시

---

### Phase 5: 핵심 기능 모듈 이식 (4~5시간) ⭐ 최대 작업

**목표**: 177개 함수를 모듈별로 분리 및 비동기 전환

**작업 순서 (의존성 기반)**:

#### 5-1. 유틸리티 (선행 필수)
- `날짜유틸_시간외.js` — 날짜 계산, 요일, 공휴일 판단
- `포맷유틸_시간외.js` — 시간 포맷, 금액 포맷, 시간 입력 처리
- `인쇄유틸_시간외.js` — 인쇄 기능

#### 5-2. 설정 모듈
- `공휴일_시간외.js` — 공휴일 로드/저장/UI (20년치 고정 + 변동)
- `유형설정_시간외.js` — 시간외 유형 on/off
- `제한설정_시간외.js` — 주/월 제한시간

#### 5-3. 근태 모듈
- `근태등록_시간외.js` — 붙여넣기 파싱, 직원 매칭, 저장
- `근태달력_시간외.js` — 달력 뷰, 수정/삭제

#### 5-4. 시간외근무 모듈
- `연장근로등록_시간외.js` — 일별 등록, 일괄 입력, 달력 연동
- `주간현황_시간외.js` — 주간 12시간 초과 감지
- `월별집계_시간외.js` — 월별 수당 집계
- `보고서_시간외.js` — 상세 보고서, 대휴 보고서, 엑셀 다운로드

**핵심 변환 패턴**:
```javascript
// Before (localStorage 직접 접근)
const data = JSON.parse(localStorage.getItem('hr_overtime_daily') || '{}');
localStorage.setItem('hr_overtime_daily', JSON.stringify(data));

// After (IPC 기반 비동기)
const data = await OvertimeDB.loadOvertimeRecords();
await OvertimeDB.saveOvertimeRecords(data);
```

```javascript
// Before (인사 데이터 직접 접근)
const empData = JSON.parse(localStorage.getItem('hr_system_v25_db') || '{}');
const employees = empData.employees || [];

// After (인사앱 데이터 읽기 전용)
const employees = await HRDataReader.getActiveEmployees();
```

**✅ 완료 기준**: 모든 6개 탭 기능이 데스크탑 앱에서 정상 동작

---

### Phase 6: 백업/복원 및 시스템 기능 (1~2시간)

**목표**: 파일 기반 백업/복원 + 인사앱 연동 상태 표시

**작업 내용**:
1. `백업_시간외.js` — Electron dialog 기반 파일 저장
2. `복원_시간외.js` — Electron dialog 기반 파일 열기
3. 설정 탭 시스템 정보 패널 업데이트
4. 인사앱 연동 상태 표시 (연결됨/끊김)
5. 자동 백업 기능 (앱 종료 시)

**핵심 변경**:
```javascript
// Before (브라우저 다운로드)
const blob = new Blob([jsonStr], { type: 'application/json' });
const a = document.createElement('a');
a.href = URL.createObjectURL(blob);
a.download = filename;

// After (Electron 파일 다이얼로그)
await window.electronAPI.exportFile(jsonStr, filename);
// main.js에서 dialog.showSaveDialog() 사용

// Before (파일 input)
<input type="file" onchange="restoreFromFile(event)">

// After (Electron 파일 다이얼로그)
const fileContent = await window.electronAPI.importFile([
    { name: 'JSON 파일', extensions: ['json'] }
]);
```

**✅ 완료 기준**: 백업 파일 저장/복원이 Electron 다이얼로그로 정상 동작

---

### Phase 7: 테스트 및 버그 수정 (2~3시간)

**목표**: 전체 기능 통합 테스트

**테스트 체크리스트**:

| # | 테스트 항목 | 상태 |
|---|---|---|
| 1 | 인사앱 미설치 시 차단 화면 | ⬜ |
| 2 | 라이선스 미인증 시 차단 | ⬜ |
| 3 | 유효 라이선스로 앱 진입 | ⬜ |
| 4 | 인사앱 직원 목록 정상 표시 | ⬜ |
| 5 | 근태 등록 (붙여넣기 파싱) | ⬜ |
| 6 | 근태 달력 뷰 / 수정 / 삭제 | ⬜ |
| 7 | 연장근로 일별 등록 / 일괄 입력 | ⬜ |
| 8 | 연장근로 달력 연동 | ⬜ |
| 9 | 주간 현황 (12시간 초과 감지) | ⬜ |
| 10 | 월별 집계 | ⬜ |
| 11 | 상세 보고서 / 대휴 보고서 | ⬜ |
| 12 | 엑셀 다운로드 | ⬜ |
| 13 | 인쇄 기능 | ⬜ |
| 14 | 설정 저장/로드 | ⬜ |
| 15 | 백업/복원 (Electron 다이얼로그) | ⬜ |
| 16 | Ctrl+S 저장 단축키 | ⬜ |
| 17 | F12 비활성화 | ⬜ |
| 18 | 앱 종료 시 자동 백업 | ⬜ |
| 19 | 오프라인 모드 (라이선스 캐시) | ⬜ |
| 20 | 인사앱 데이터 변경 시 반영 | ⬜ |

**✅ 완료 기준**: 모든 테스트 항목 통과

---

### Phase 8: 빌드 및 배포 (1~2시간)

**목표**: 설치 파일 생성 및 GitHub Release

**작업 내용**:
1. `electron-builder` 설정 (package.json)
2. 앱 아이콘 설정
3. 자동 업데이트 설정 (electron-updater)
4. `npm run build` → exe 생성
5. GitHub 저장소 생성 (overtime-system)
6. GitHub Release 발행

**package.json 빌드 설정**:
```json
{
  "name": "overtime-system",
  "productName": "시간외근무관리",
  "version": "1.0.0",
  "main": "main.js",
  "build": {
    "appId": "com.hr.overtime",
    "productName": "시간외근무관리",
    "win": {
      "target": "nsis",
      "icon": "assets/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "installerLanguages": ["ko"]
    },
    "publish": {
      "provider": "github",
      "owner": "mangfeel",
      "repo": "overtime-system"
    }
  }
}
```

**배포 절차** (인사관리 앱과 동일 패턴):
```powershell
# 1단계: Git 커밋 & 빌드
cd C:\overtime-system
git add .
git commit -m "feat: 초기 릴리즈 v1.0.0"
git push origin main
npm run build

# 2단계: Tag 생성 & Push
git tag v1.0.0
git push origin v1.0.0

# 3단계: 파일명 변경
cd C:\overtime-system\dist
Rename-Item "시간외근무관리 Setup 1.0.0.exe" "OT-System-Setup-1.0.0.exe"
Rename-Item "시간외근무관리 Setup 1.0.0.exe.blockmap" "OT-System-Setup-1.0.0.exe.blockmap"

# 4단계: GitHub Release 생성
# Tag: v1.0.0
# Title: v1.0.0 - 시간외근무관리 초기 릴리즈
# 업로드: OT-System-Setup-1.0.0.exe, .blockmap, latest.yml
```

**✅ 완료 기준**: 설치 파일로 다른 PC에 설치/실행 성공

---

## ⚠️ 주요 기술 변환 포인트

### 1. localStorage → IPC 파일 I/O (비동기 전환)

원본 웹앱의 모든 `localStorage` 호출을 `async/await` 기반 IPC로 변환해야 합니다.

```javascript
// ❌ Before: 동기 방식
function saveBulkInput() {
    const data = JSON.parse(localStorage.getItem(OVERTIME_DAILY_KEY) || '{}');
    // ... 처리 ...
    localStorage.setItem(OVERTIME_DAILY_KEY, JSON.stringify(data));
}

// ✅ After: 비동기 방식
async function saveBulkInput() {
    const data = await OvertimeDB.loadOvertimeRecords();
    // ... 처리 ...
    await OvertimeDB.saveOvertimeRecords(data);
}
```

### 2. 인사 데이터 참조 패턴

```javascript
// ❌ Before: 같은 localStorage에서 직접 읽기
const hrData = JSON.parse(localStorage.getItem('hr_system_v25_db') || '{}');
const employees = hrData.employees || [];

// ✅ After: 인사앱 데이터 전용 리더 사용
const employees = await HRDataReader.getActiveEmployees();
if (!employees || employees.length === 0) {
    alert('인사관리 시스템에서 직원 데이터를 찾을 수 없습니다.');
    return;
}
```

### 3. 파일 다운로드/업로드 → Electron Dialog

```javascript
// ❌ Before: 브라우저 Blob 다운로드
const blob = new Blob([content]);
const a = document.createElement('a');
a.href = URL.createObjectURL(blob);
a.download = 'file.xlsx';
a.click();

// ✅ After: Electron 다이얼로그
await window.electronAPI.exportFile(content, 'file.xlsx');
```

### 4. prompt() 제거

원본 코드에 `prompt()` 사용 여부 확인 → **없음** ✅ (confirm/alert만 사용)

### 5. ExcelJS CDN → npm 패키지

```html
<!-- ❌ Before: CDN -->
<script src="https://cdn.jsdelivr.net/npm/exceljs@4.3.0/dist/exceljs.min.js"></script>

<!-- ✅ After: npm 패키지 (preload.js에서 require 또는 번들링) -->
```

---

## 📊 원본 코드 분석 요약

### 전체 구조

| 항목 | 수치 |
|------|------|
| 총 줄 수 | 11,710줄 |
| 함수 수 | 177개 |
| CSS 줄 수 | ~660줄 (인라인) |
| HTML 줄 수 | ~750줄 |
| JavaScript 줄 수 | ~10,300줄 |
| localStorage 키 | 11개 (자체 5개 + 인사앱 6개) |
| CDN 의존성 | ExcelJS 1개 |

### 기능별 함수 분포

| 모듈 | 함수 수 | 예상 줄 수 |
|------|---------|-----------|
| 초기화/탭 전환/단축키 | ~10 | ~300 |
| 근태 등록/파싱/매칭 | ~20 | ~1,200 |
| 근태 달력/수정/삭제 | ~15 | ~800 |
| 연장근로 등록/일괄입력 | ~35 | ~2,800 |
| 주간현황 | ~8 | ~600 |
| 월별집계 | ~5 | ~400 |
| 보고서/엑셀 | ~15 | ~1,800 |
| 급여계산 참조 | ~10 | ~800 |
| 공휴일/설정 | ~12 | ~500 |
| 백업/복원 | ~5 | ~300 |
| 유틸리티 (포맷/날짜) | ~15 | ~400 |
| 나머지 | ~27 | ~700 |

---

## 🔄 버전 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| v1.0 | 2026-02-05 | 초기 로드맵 작성 |
